# Инициатива создания децентрализованной автономной внутрипартийной организации (ДАВО) "Заря" для партии "Рассвет"

`Версия: Челябинск`

Документ адресован: директору МО в г. Челябинске Партии "Рассвет" Даниилу Дмитриевичу Бурцеву. 

## Предисловие

Уважаемый Даниил Дмитриевич,

Вашему вниманию представляется документ, который описывает теоретическое распределённое программное обеспечение спроектированное для реализации и автоматизации управления мнением партии. Целью этого документа является донесение до Совета МО Челябинской области партии "Рассвет" ценности инициативы по разработке демо этой системы, а также в дальнейшем разработки полноценной и безопасной её версии.

## Презентация для Центрального Совета Партии "Рассвет"

Документ публичен и распространяется согласно лицензии указанной в репозитории. Лицензия на оформление презентации указана на её последнем слайде. 

Доступ: **https://docs.google.com/presentation/d/1mRsgTg3XsrVSvXpRoXnVyLOgX2QSJKPEfJUwnsZz_Uk/edit?usp=sharing**

## Глоссарий

### Основные понятия

- **ДАВО "Заря" или "Заря"** - распределённое ПО, для наблюдения, формирования и анализа мнения партии "Рассвет".

- **Мнение партии ($𝓜$)** — формализованное представление совокупной позиции партии как кортежа двух матриц:
  - $𝓧$ — непрерывные случайные величины (количественные показатели),
  - $𝓨$ — дискретные случайные величины (категориальные оценки).

- **Матрица $𝓢_X$, $𝓢_Y$** — выборочные реализации значений в ячейках. Хранят значения, полученные через голосование DAO.

- **Агрегированное мнение ($Ĥ_𝓜$)** — результат применения агрегирующей функции $f(𝓢_X, 𝓢_Y)$, используемый для принятия решений и генерации текстов.

---

### Структура строк в матрицах внутри $𝓜$ и их кодов

- Строка каждой ячейки оформлена как: `<Тематика> — <Код органа>`
  - `СЗД` — общепартийный уровень (Съезд),
  - `СОВ` — уровень Центрального совета,
  - `ПРЛ` — уровень председателя,
  - `НН` — субъект РФ (регион под номером НН),
  - `НН.Х.ОБС` — Общее собрание МО под номером Х в регионе НН,
  - `НН.Х.СОВ` — Совет МО под номером Х в регионе НН,
  - `НН.СОВ` — Совет регионального отделения региона под номером НН,
  - `НН.КОН` — Конференция РО под номером НН.
  - `НН.ОБС` — Общее собрание РО под номером НН.

---

### DAO (Децентрализованная автономная организация) и голосование

- **Смарт-контракт** - Кусок кода, который хранится, исполняется и верифицируется в блокчейне.

- **Смарт-контракт `VotingRouter`** — определяет, какой орган должен голосовать по изменению в ячейке.

- **Смарт-контракт `VotingModule`** — универсальный контракт голосования, параметризуемый кворумом, типом голосования и правилом принятия решений.

- **Смарт-контракт `MatrixStorage`** — хранилище выборок $S_X$, $S_Y$.

- **Смарт-контракт `MatrixSchemaRegistry`** — хранит семантику строк и столбцов матрицы, в том числе код органа.

- **Смарт-контракт `OrganRegistry`** — регистрирует все органы DAO, их участников и типы. Делегирует голосование.

- **Смарт-контракт `PartyMembershipRegistry`** — глобальный реестр всех участников партии, используемый для валидации права голосовать.

---

### Процедуры

- **Жизненный цикл ячейки** — путь от предложения нового значения → голосования → добавления значения в выборку.

- **Предложение** — содержит одно значение, тип ячейки (из $X$ или из $Y$), координаты и автоматически фиксируемую метку времени.

- **Голосование** — проходит через DAO-механику, орган определяется по строке ячейки и регламентируется уставом.

- **Агрегация** — после пополнения выборки происходит перерасчёт агрегированных значений (например, медиана, мода, доверительный интервал).

---

### Человекочитаемое мнение

- **Шаблон (Template)** — фрагмент текстового документа, в который подставляется значение из $Ĥ_𝓜$.

- **Человекочитаемая репрезентация** — автоматически сгенерированный фрагмент текста, отражающий текущую позицию DAO по конкретной ячейке.

- **Однозначность интерпретации** — гарантируется через `MatrixSchemaRegistry`, структурированный шаблон и детерминированную агрегацию.

---

### Прогнозирование и анализ

- **Нулевая гипотеза (`H₀`)** — проверка, изменится ли агрегированное мнение после добавления нового значения.

- **Анализ поведения `𝓜(t)`** — отслеживание динамики мнения партии во времени.

- **Фракционный анализ** — выявление групп DAO-участников, формирующих устойчивые паттерны голосования или оценок.

- **Показатели стабильности** — измерения колебаний, консенсуса и поляризации.

- **Сценарный анализ и стресс-тесты** — прогноз последствий, если несколько значений будут одновременно предложены или приняты.

## Мотивация

Современные партии располагают тоннами текста помимо Устава, Программы и Декларации ценностей в том или ином её виде. Из-за чего тяжело миновать разного рода боли, которые могут беспокоить любого члена партии. Например:

1) Чтобы партийным должностным лицам с широким кругом полномочий принимать решения качественно и понимая мнение партии верно, им необходимы консультации с коллегами или независимыми специалистами. Размеры эти консультаций по участникам и длительность их, в общем случае, непредсказуемы и могут быть избыточными или наоборот слишком бессодержательными. Но даже при всём при этом, остаётся шанс совершить ошибку.
2) Каждый раз, чтобы понять любой аспект свежего мнения партии (напр. Чтобы инициировать голосование или принять решение на месте), необходимо созывать соответсвующие управляющие органы партии. Это создаёт дополнительную временную и бюрократическую нагрузку.
3) Каждый документ порождаемый коллективной волей партии и основанный на её мнении представляет из себя поправку. Каждая новая поправка - это головная боль для любого члена партии, который не хочет бегать по всей истории вопроса, а лишь хочет понять текущее состоянее дел.

Каждая из перечисленных болей, может решаться "Зарёй" таким образом:

1) "Заря" агрегирует всю информацию по любому делу или предмету обсуждения учитывая одновременно как фактический так и вероятностный его аспект. Поэтому она может давать рекомендации, которые могут заменить некоторые из необходимых для принятия решения телодвижений. 
   1) Например, вышел новый закон, нарушающий естественные права граждан. Вы, директор МО "Рассвет". Вместо того, чтобы задавать вопрос, как члены партии зарегистрированные в МО видят этот закон, Вы идёте в "Зарю", запрашиваете статистически обоснованную оценку мнения с наперёд заданной точностью в 80%. "Заря" делает агрегацию мнения, и выдаёт какую-то конкретную качественную или количественную характеристику того, как себя могут почувствовать члены партии узнав об этом законе с вероятностью 80%. Следовательно, Вы реформируете свой вопрос Совету или Общему собранию МО из более общего, в более конкретный, экономя время на уточняющих вопросах и поправках к формулировкам вопроса.
2) Рекомендация "Зари" даёт возможность качественно фильтровать вопросы органам партии, для того чтобы понимать эти самые аспекты мнения партии. А иногда, рекомендация может совсем снимать необходимость созыва этих самых органов, для задавания вопросов, не порождая вторую тонну документации.
3) После каждой рекомендации "Заря" имеет возможность учитывать реакцию на эту рекомендацию, каждый раз дообучаясь понимать мнение партии. Поэтому любой член партии, может дать "запрос" в "Зарю" и получить как самое свежее мнение партии, так и историю его изменения. 

### А в итоге, зачем это нужно партии «Рассвет»?

У партии «Рассвет» — широкий спектр инициатив: от налоговой политики и поддержки образования до рекультивации свалок, цифровой трансформации и вопросов самоорганизации на местах. Каждая из этих тем:
- по-разному воспринимается в регионах и МО;
- отличается степенью реализуемости;
- вызывает разные оценки — и эмоциональные, и количественные.

В классической партийной системе всё это утопает в бесконечных поправках, субъективных спорах и протоколах, понятных только юристам. Партия «Рассвет» идёт дальше.

Система ДАВО "Заря" — это:

#### Математически формализованное мнение партии

Мнение представлено в виде матрицы вероятностных оценок:
- количественных (например, «оптимальный налог — 13%»),
- качественных («инициатива приемлема», «мнение — сомнительно»),
- агрегированных на основе голосований.

---

#### Прогнозируемое управление

Каждое новое голосование не просто добавляет значение в матрицу — оно может изменить всё мнение.  
Перед тем как голосовать, можно узнать:
- приведёт ли новое значение к изменению официальной позиции;
- насколько партия склонна к принятию или отклонению;
- сохранится ли нулевая гипотеза, т.е. "партия по-прежнему считает X".

Это делает принятие решений осмысленным, а не импульсивным.

---

#### Многоуровневая система голосования

Каждая ячейка матрицы привязана к конкретному уставному органу:
- МО, если вопрос локальный;
- Конференции или Совету региона — если региональный;
- Съезду — если речь идёт о позиции всей партии.

DAO обеспечивает жёсткое соответствие Уставу и фиксирует:
- кто голосует;
- когда;
- с каким результатом.

---

#### Аналитика и контроль эволюции мнения

Мнение партии — живое. Система позволяет:
- отслеживать его изменения во времени,
- выявлять устойчивые и нестабильные темы,
- видеть зоны согласия и поляризации,
- прогнозировать, где и когда могут возникнуть внутренние фракции.

---

#### Человекочитаемые документы без поправок

Тексты формируются автоматически из текущего мнения:
- без ручной редакторской суеты;
- без конфликта версий;
- всегда свежие и однозначные.

Если мнение изменилось — изменилась и строчка в документе. А значит — нет рассогласования между тем, что партия говорит, и тем, что она думает.

---

#### Подводя итог

Система "Заря" позволяет:
- продвигать не просто инициативы, а разумные инициативы;
- избегать популизма и утопий;
- делать мнение партии верифицируемым, прогнозируемым и справедливым;
- а самое главное — создавать партию, которая действительно думает.

## Формальная вероятностная модель мнения партии

Как оцифровать неоцифруемое? Статистика!

Мнение партии $\mathcal{M}$ формализуется как кортеж из двух матриц случайных величин:

$$
\mathcal{M} = (\mathbf{X}, \mathbf{Y})
$$

где:
- $\mathbf{X} \in \mathbb{R}^{m \times n}$ — матрица **непрерывных случайных величин**. Каждая ячейка $X_{i,j}$ описывает количественную оценку или показатель, важный для идеологической или программной позиции партии.
- $\mathbf{Y} \in \mathbb{Z}^{p \times q}$ — матрица **дискретных случайных величин**, описывающих качественные, категориальные или оценочные суждения партии по соответствующим вопросам.

Каждая ячейка матрицы содержит параметры случайной величины:
- для $\mathbf{X}$: $X_{i,j} \sim \mathcal{D}_{i,j}(\theta)$ — нормальное, логнормальное, бета и т.д.
- для $\mathbf{Y}$: $Y_{k,l} \sim \text{Categorical}(p_1, ..., p_r)$ — вероятности по категориям мнений.

Модель $\mathcal{M}$ может изменяться во времени: 

$$
\mathcal{M_t} \rightarrow \mathcal{M_{t+1}} = \mathcal{M_t} + \Delta\mathcal{M}
$$

Это конечно очень широкое определение, однако в реальной жизни, мы не можем знать априори параметры случайных величин. Поэтому необходимо перейти от формальной модели к эмпирической.

### Обоснование перехода к эмпирической модели

Формальная модель мнения партии, представленная как кортеж параметризованных распределений, теоретически точна и математически строгая. Однако при реализации жизненного цикла $M$ в инфраструктуре DAO в блокчейне, и например, на языке Solidity возникает ряд ограничений:

- Невозможно хранить непрерывные распределения непосредственно в on-chain (в блокчейне) памяти без значительных затрат.
- Отсутствие нативной поддержки вещественных чисел в Solidity ограничивает работу с параметрами распределений.
- Участие членов DAO, голосующих за значения и оценки, формирует выборки, а не готовые распределения.

В этой связи более реалистичным и практически пригодным подходом становится использование **накапливаемых выборок**. Таким образом, ячейки матриц мнения хранят не априорно заданные распределения, а выборки значений, которые можно агрегировать и аппроксимировать off-chain (на сервере) или по запросу on-chain.

Это позволяет связать формальную модель с технической реализацией в рамках DAO-процессов, не теряя смысловой строгости и соответствия исходной структуре.

## Примеры матриц эмпирической модели

Получается, что матрица $M$ теперь выглядит так: 

$$
M = (S_X, S_Y)
$$

где:
- $S_X$ - непрерывная половина мнения партии (или матрица выборок непрерывных случайных величин),
- $S_Y$ - дискретная или категориальная половина мнения партии (или матрица выборок категориальных случайных величин).

### Матрица выборок непрерывных случайных величин ($S_X$)

| Показатель                                      | Примеры значений выборки    |
| ----------------------------------------------- | --------------------------- |
| Уровень безработицы в регионах (%)              | `{5.4, 5.6, 6.0, 4.9, 5.2}` |
| Доля бюджета, направленная на образование (%)   | `{8.1, 8.3, 7.9, 8.0, 8.2}` |
| Оценка экологического ущерба (баллы от 0 до 10) | `{6.7, 6.5, 6.9, 7.1, 6.8}` |

#### Интерпретация ($S_X$):

**Уровень безработицы в регионах:**

$$
S_X[0][0] = {5.4, 5.6, 6.0, 4.9, 5.2}
$$

**Доля бюджета на образование:**  

$$
S_X[1][1] = {8.1, 8.3, 7.9, 8.0, 8.2}
$$

**Экологический ущерб (0–10):**  

$$
S_X[2][2] = {6.7, 6.5, 6.9, 7.1, 6.8}
$$

### Матрица выборок категориальных случайных величин ($S_Y$)

| Вопрос оценки                                       | Возможные категории                              | Примеры выборки                                           |
| --------------------------------------------------- | ------------------------------------------------ | --------------------------------------------------------- |
| Оценка инициативы об увеличении учительских зарплат | {«поддерживается», «сомнительно», «отвергается»} | `{«поддерживается», «поддерживается», «сомнительно»}`     |
| Отношение к налоговой реформе                       | {«желательно», «приемлемо», «неприемлемо»}       | `{«приемлемо», «желательно», «приемлемо», «неприемлемо»}` |
| Мнение о модели базового дохода                     | {«нужна», «сомнительна», «опасна»}               | `{«нужна», «сомнительна», «нужна», «опасна»}`             |

#### Интерпретация ($S_Y$):

**Инициатива о повышении зарплат учителям:**

$$
S_Y[0][0] = {"поддерживается", "поддерживается", "сомнительно"}
$$

**Отношение к налоговой реформе:**  

$$
S_Y[1][2] = {"приемлемо", "желательно", "приемлемо", "неприемлемо"}
$$

**Модель базового дохода:**  

$$
S_Y[3][1] = {"нужна", "сомнительна", "нужна", "опасна"}
$$

### Сводная формула и определение агрегирующей функции $f$

Хорошо, имеем эти матрицы. А как из них сделать какие-никакие выводы?

$$
M = (S_X, S_Y)
$$

$$
\hat{M} = f(S_X, S_Y)
$$

Где $f$ — это функция агрегирования, которая строит оценку текущего мнения партии на основании накопленных выборок. Эта функция применяется:

1. **В рамках внутренних процессов партии**: когда необходимо определить доминирующее мнение по инициативе, теме или показателю.
2. **При принятии решений согласно Уставу**: агрегированное значение или распределение может служить основанием для проведения голосования съездом, конференцией или советом.

#### Формальное определение функции $f$

Функция $f$ определяется как преобразование:

$f : (S_X, S_Y) → (\hat{X}, \hat{Y})$


Где:
- $\hat{X}_{i,j} = \text{Estimate}(S_X[i][j])$ — оценка распределения (например, метод моментов, медиана, дисперсия) или агрегированная величина (например, среднее значение, доверительный интервал).
- $\hat{Y}_{k,l} = \text{Histogram}(S_Y[k][l])$ — оценка вероятностей категорий по частотам выборки.

#### Пример использования в процедурах DAO:
- Если изменение мнения $\hat{M} \rightarrow \hat{M}'$ (изменение агрегированной оценки мнения) приводит к отклонению от предыдущего состояния больше определённого порога (в соответствии с логикой Устава), инициируется процедура голосования.
- Порог может зависеть от веса оценки (например, по числу голосов, охвату, доверительной ширине распределения).
- Решение, принятое съездом, фиксирует новое агрегированное мнение партии, заменяя соответствующие ячейки в $M$.

Таким образом, $f$ является связующим звеном между выборочной динамикой мнения партии и институциональным механизмом принятия решений, закреплённым в Уставе партии.

## Децентрализованная автономная организация (DAO)

Предлагаемая система DAO должна отражать организационную структуру и порядок принятия решений, которые строго соответствуют Уставу партии «Рассвет» и его духу. Поэтому все изменения матрицы мнения $\mathcal{M}$ допускаются **только через голосование**, и только тем органом, который уполномочен на основании уставного кодирования строки ячейки.

### Архитектура смарт-контрактов

Система реализована как **монолитный смарт-контракт** `Zarya.sol` с подключаемыми библиотеками. Такой подход обеспечивает:
- атомарность операций;
- единую точку входа для всех взаимодействий;
- упрощённую верификацию состояния системы;
- снижение gas-затрат за счёт отсутствия межконтрактных вызовов.

### Основные компоненты системы

#### 1. Библиотека `Matricies.sol`
**Назначение:** Управление матрицей мнения партии $\mathcal{M} = (S_X, S_Y)$

**Ключевые структуры:**
- `CategoricalCell` — ячейка категориальной матрицы $S_Y[k][l]$:
  - `organ: PartyOrgan` — уполномоченный орган управления
  - `categoricalSample: Checkpoints.Trace224` — временной ряд значений выборки
  - `allowedCategories: EnumerableSet.UintSet` — множество допустимых категорий
  - `categoryNames: mapping(uint64 => string)` — человекочитаемые названия категорий

- `NumericalCell` — ячейка числовой матрицы $S_X[i][j]$:
  - `organ: PartyOrgan` — уполномоченный орган управления
  - `numericalSample: Checkpoints.Trace224` — временной ряд числовых значений
  - `decimals: uint8` — точность представления (количество знаков после запятой)

- `PairOfMatricies` — глобальное хранилище:
  - `themes: mapping(bool => mapping(uint256 => string))` — темы строк матрицы
  - `statements: mapping(bool => mapping(uint256 => string))` — описания столбцов
  - `categoricalMatrix: mapping(uint256 => mapping(uint256 => CategoricalCell))`
  - `numericalMatrix: mapping(uint256 => mapping(uint256 => NumericalCell))`

**Особенность хранения значений:**  
Используется структура `Checkpoints.Trace224` из OpenZeppelin, которая упаковывает в 224 бита:
- верхние 160 бит: адрес автора значения
- нижние 64 бита: само значение
- дополнительно: 32-битная метка времени

Это обеспечивает:
- автоматическую временную индексацию;
- возможность ретроспективного анализа;
- атрибуцию каждого значения автору;
- эффективное использование storage.

---

#### 2. Библиотека `PartyOrgans.sol`
**Назначение:** Типизация и идентификация партийных органов

**Основной тип:** `PartyOrgan` — user-defined value type на основе `bytes32`  
Каждый орган получает уникальный идентификатор через `keccak256` от строкового представления.

**Типы органов:**
```solidity
enum PartyOrganType {
    LocalSoviet,              // НН.Х.СОВ
    LocalGeneralAssembly,     // НН.Х.ОБС
    RegionalSoviet,           // НН.СОВ
    RegionalConference,       // НН.КОН
    RegionalGeneralAssembly,  // НН.ОБС
    Chairperson,              // ПРЛ
    CentralSoviet,            // СОВ
    Congress                  // СЗД
}
```

**Структура реестра:**
```solidity
struct MembersRegistry {
    mapping(PartyOrgan => EnumerableSet.AddressSet members) membersByOrgan;
}
```

**Генерация идентификаторов:**
Функция `from(PartyOrganType, Region, uint256)` формирует строковый код органа и хэширует его:
- `"74.2.СОВ"` → `keccak256("74.2.СОВ")` → `PartyOrgan`
- `"СЗД"` → `keccak256("СЗД")` → `PartyOrgan`

Это обеспечивает:
- детерминированность идентификаторов;
- совместимость с off-chain системами через строковые коды;
- газоэффективное сравнение (32 байта вместо строк).

---

#### 3. Библиотека `Votings.sol`
**Назначение:** Управление жизненным циклом голосований

**Типы голосований:**
```solidity
enum VotingType {
    MembershipVoting,          // Приём в члены партии
    CategoricalValueVoting,    // Добавление значения в S_Y
    NumericalValueVoting,      // Добавление значения в S_X
    CategoryVoting,            // Добавление новой категории
    DecimalsVoting,            // Установка точности числовой ячейки
    ThemeVoting,               // Добавление темы (строки матрицы)
    StatementVoting            // Добавление описания (столбца матрицы)
}
```

**Структура голосования:**
```solidity
struct Voting {
    uint256 id;
    address initiator;
    uint256 deadline;
    VotingType votingType;
    PartyOrgan organ;
    // ... специфичные для типа данные
    mapping(address => bool) votes;
    mapping(address => bool) hasVoted;
    uint256 votesFor;
    uint256 votesAgainst;
    bool finalized;
}
```

**Жизненный цикл:**
1. Создание (`createXXXVoting`) — инициатор должен быть членом органа
2. Голосование (`castVote`) — только члены соответствующего органа
3. Исполнение (`executeVoting`) — проверка кворума и процента одобрения
4. Финализация — при успехе изменения применяются к матрице

---

#### 4. Библиотека `Regions.sol`
**Назначение:** Типизация субъектов РФ

**Основной тип:** `Region` — user-defined value type на основе `uint8`

Содержит все 89 субъектов РФ (включая новые регионы):
```solidity
Region public constant Moscow = Region.wrap(77);
Region public constant Chelyabinsk = Region.wrap(74);
Region public constant DPR = Region.wrap(150);
// ... и т.д.
```

Функция `toString()` преобразует код региона в строку для формирования кодов органов.

---

#### 5. Главный контракт `Zarya.sol`
**Назначение:** Единая точка входа и координация всех компонентов

**Внутреннее состояние:**
```solidity
contract Zarya {
    uint256 public nextVotingId;
    mapping(uint256 => Votings.Voting) internal _votings;
    PartyOrgans.MembersRegistry internal _partyMembersRegistry;
    Matricies.PairOfMatricies internal _matricies;
}
```

**Основные функции:**

*Создание голосований:*
- `createMembershipVoting(organ, member, duration)` — может быть вызвано членом органа или Председателем
- `createCategoryVoting(organ, x, y, category, duration)` — требует членства в органе
- `createDecimalsVoting(organ, x, y, decimals, duration)` — требует членства в органе
- `createThemeVoting(isCategorical, x, theme, duration)` — может быть вызвано любым участником
- `createStatementVoting(isCategorical, x, y, statement, duration)` — может быть вызвано любым участником
- `createCategoricalValueVoting(organ, x, y, value, valueAuthor, duration)` — требует членства в органе
- `createNumericalValueVoting(organ, x, y, value, valueAuthor, duration)` — требует членства в органе

*Участие в голосовании:*
- `castVote(votingId, support, organ)` — требует членства в органе

*Исполнение:*
- `executeVoting(votingId, minimumQuorum, minimumApprovalPercentage)` — применяет изменения при успехе

*Просмотр состояния:*
- Результаты голосований: `getVotingResults()`, `hasVoted()`, `isVotingActive()`
- Матрица мнения: `getTheme()`, `getStatement()`, `getCellOrgan()`, `getCellInfo()`
- Категории: `getAllowedCategories()`, `getCategoryName()`, `isCategoryAllowed()`

### Взаимодействие компонентов

```text
┌─────────────────────────────────────────────────┐
│              Контракт Zarya.sol                 │
│  (единая точка входа, координация компонентов)  │
└────────────┬────────────────────────────────────┘
             │
   ┌─────────┼─────────┬─────────────┬────────────┐
   │         │         │             │            │
   ▼         ▼         ▼             ▼            ▼
┌──────┐ ┌───────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐
│Votings│ │Matricies│ │PartyOrgans│ │Regions  │ │OpenZep- │
│.sol   │ │.sol    │ │.sol       │ │.sol     │ │pelin    │
└───┬───┘ └───┬────┘ └─────┬─────┘ └────┬─────┘ └─────┬───┘
    │         │            │            │            │
    │    Управление       Типы      Коды        Checkpoints
    │    матрицей M    и реестр    регионов    EnumerableSet
    │                   органов
    │
Жизненный цикл
голосований
```

**Поток данных при добавлении значения:**

1. Участник вызывает `createCategoricalValueVoting()` в `Zarya`
2. `Zarya` проверяет членство через `PartyOrgans.MembersRegistry`
3. Создаётся `Voting` с типом `CategoricalValueVoting`
4. Участники органа голосуют через `castVote()`
5. При `executeVoting()`:
   - проверяется кворум и процент одобрения
   - если успешно → вызывается `Matricies.addValue()`
   - значение упаковывается в `Checkpoints.Trace224`
   - добавляется в выборку соответствующей ячейки
6. Событие `ValueAdded` эмитируется для off-chain индексации

---

### Поддержка всех уставных органов

| Орган                                  | Код        | Тип в PartyOrganType      | Представляет                     |
| -------------------------------------- | ---------- | ------------------------- | -------------------------------- |
| Съезд партии                           | `СЗД`      | `Congress`                | Делегаты от >50% субъектов РФ    |
| Председатель                           | `ПРЛ`      | `Chairperson`             | Единоличный исполнительный орган |
| Центральный совет                      | `СОВ`      | `CentralSoviet`           | Члены Центрального совета        |
| Региональная конференция               | `НН.КОН`   | `RegionalConference`      | Делегаты от >50% МО в регионе    |
| Общее собрание регионального отделения | `НН.ОБС`   | `RegionalGeneralAssembly` | Все члены РО                     |
| Совет регионального отделения          | `НН.СОВ`   | `RegionalSoviet`          | Совет РО (если избран)           |
| Общее собрание местного отделения      | `НН.Х.ОБС` | `LocalGeneralAssembly`    | Все члены МО                     |
| Совет местного отделения               | `НН.Х.СОВ` | `LocalSoviet`             | Совет МО (если избран)           |

**Формирование идентификатора органа:**

Для генерации уникального `PartyOrgan` используется функция:
```solidity
PartyOrgan organ = PartyOrgans.from(
    PartyOrganType.LocalSoviet,  // тип органа
    Regions.Chelyabinsk,         // регион (74)
    2                             // номер МО
);
// Результат: keccak256("74.2.СОВ")
```

Строковые коды формируются автоматически на основе правил:
- федеральный уровень: только постфикс (`СЗД`, `СОВ`, `ПРЛ`)
- региональный уровень: `НН.ПОСТФИКС` (например, `74.СОВ`)
- местный уровень: `НН.Х.ПОСТФИКС` (например, `74.2.ОБС`)

---

### 📌 Инварианты системы

1. **Изменения только через голосование**  
   Любое изменение $\mathcal{M}$ требует успешного завершения голосования.

2. **Соответствие органа ячейке**  
   Каждая ячейка `CategoricalCell` или `NumericalCell` содержит поле `organ: PartyOrgan`.  
   Голосовать может только тот орган, который указан в ячейке.

3. **Верификация членства**  
   Участие в голосовании требует наличия адреса в `MembersRegistry` для соответствующего органа.
   
   *Исключение:* Председатель (Chairperson) имеет право инициировать голосования о членстве (`createMembershipVoting`) для любого органа, даже если он не является членом этого органа. Это обеспечивает надзорную функцию единоличного исполнительного органа партии.

4. **Атомарность операций**  
   Монолитная архитектура обеспечивает, что голосование и обновление матрицы происходят атомарно.

5. **Иммутабельность истории**  
   Использование `Checkpoints.Trace224` гарантирует, что все добавленные значения сохраняются с метками времени и не могут быть изменены.

6. **Типобезопасность**  
   User-defined types (`PartyOrgan`, `Region`) предотвращают случайное смешивание несовместимых данных.

---

### Преимущества архитектуры

**Газоэффективность:**
- отсутствие межконтрактных вызовов снижает стоимость транзакций
- упаковка данных в `Checkpoints` экономит storage

**Верифицируемость:**
- единое состояние в одном контракте упрощает аудит
- все изменения трассируются через события

**Масштабируемость:**
- библиотечная структура позволяет безопасно расширять функциональность
- добавление новых типов голосований не требует изменения core-логики

**Соответствие Уставу:**
- типизация органов напрямую отражает партийную структуру
- проверка полномочий встроена на уровне модификаторов

**Прозрачность:**
- каждое значение в матрице имеет автора и timestamp
- полная история изменений доступна для анализа

## Структура человекочитаемой интерпретации внутреннего состояния `M` (результатов Сводной функции).

Для того чтобы продемонстрировать то, как происходит генерация человекочитаемой интерпретации, я приведу несколько примеров, где демонстрируются полный циклы - от Предложения, до человекочитаемоего динамического документа.

### Принцип: единая глобальная матрица

На всю партию в рамках "Зари" существует одна глобальная матрица мнения:

$$
\mathcal{M} = (\mathbf{X}, \mathbf{Y})
$$

Все количественные и качественные показатели фиксируются в единой структуре. Разграничение регионов и местных отделений реализуется через структурированный идентификатор в строке ячейки.

**Примеры:**

| Строка                       | Интерпретация                            |
| ---------------------------- | ---------------------------------------- |
| Экология - 74.СОВ            | Позиция Совета РО по Челябинской области |
| Социальная защита - 77.3.СОВ | Совет МО №3 в Москве                     |
| Экономика - СОВ              | Федеральная позиция партии               |

### Этапы жизненного цикла ячейки

#### 1. Инициация предложения изменения

Любой участник DAO, являющийся членом соответствующего органа, может подать предложение на изменение ячейки матрицы мнения.

**Типы предложений:**

*Добавление значения в числовую ячейку:*
```solidity
uint256 votingId = zarya.createNumericalValueVoting(
    organ,        // PartyOrgan — орган, к которому относится ячейка
    x,            // uint256 — номер строки (темы)
    y,            // uint256 — номер столбца (показателя)
    value,        // uint64 — предлагаемое значение
    valueAuthor,  // address — автор значения (может отличаться от инициатора)
    duration      // uint256 — длительность голосования в секундах
);
```

*Добавление значения в категориальную ячейку:*
```solidity
uint256 votingId = zarya.createCategoricalValueVoting(
    organ,
    x,
    y,
    categoryValue,  // uint64 — код категории (должна быть в allowedCategories)
    valueAuthor,
    duration
);
```

**Автоматическая фиксация метаданных:**
- `initiator: address` — кто инициировал голосование (`msg.sender`)
- `deadline: uint256` — `block.timestamp + duration`
- `votingId: uint256` — уникальный глобальный идентификатор

**Проверки при создании:**
1. Инициатор должен быть членом указанного органа
2. Ячейка должна иметь установленные `theme` и `statement`
3. Для категориальных ячеек: категория должна быть в `allowedCategories`
4. Для числовых ячеек: должна быть установлена точность `decimals`

---

#### 2. Определение органа голосования

Орган голосования **уже известен на этапе создания предложения** — он передаётся явным параметром и должен соответствовать органу, зафиксированному в ячейке.

**Валидация соответствия:**
```solidity
// При выполнении голосования в библиотеке Matricies.sol
if (cell.organ != PartyOrgans.ZERO_PARTY_ORGAN && cell.organ != organ) {
    revert InvalidOrgan(organ);
}
```

**Логика привязки органа к ячейке:**
- Первое успешное голосование устанавливает `organ` для ячейки
- Последующие изменения возможны только тем же органом
- Это гарантирует, что каждая ячейка находится под контролем единого уставного органа

**Примеры соответствия:**

| Ячейка `X[i][j]` или `Y[k][l]` | Орган в ячейке | Кто может голосовать                   |
| ------------------------------ | -------------- | -------------------------------------- |
| `Y[5][2]`                      | `74.2.СОВ`     | Только Совет МО №2 Челябинской области |
| `X[12][7]`                     | `74.КОН`       | Только Конференция Челябинской области |
| `Y[3][1]`                      | `СЗД`          | Только Съезд партии                    |

---

#### 3. Проведение голосования

**Участие в голосовании:**
```solidity
zarya.castVote(
    votingId,   // uint256 — идентификатор голосования
    support,    // bool — true = за, false = против
    organ       // PartyOrgan — орган, от имени которого голосует участник
);
```

**Проверки при голосовании:**
1. Голосование должно быть активным (`block.timestamp < deadline`)
2. Участник должен быть членом указанного органа
3. Участник не должен был голосовать ранее
4. Орган должен соответствовать органу голосования

**Учёт голосов:**
```solidity
struct Voting {
    mapping(address => bool) votes;      // true = за, false = против
    mapping(address => bool) hasVoted;   // флаг участия
    uint256 votesFor;                     // счётчик голосов "за"
    uint256 votesAgainst;                 // счётчик голосов "против"
}
```

---

#### 4. Исполнение и финализация

**Запуск исполнения:**
```solidity
bool success = zarya.executeVoting(
    votingId,
    minimumQuorum,               // uint256 — минимальное число участников (абсолютное)
    minimumApprovalPercentage    // uint256 — минимальный процент одобрения (0-100)
);
```

**Проверки при исполнении:**
1. Голосование должно быть завершено (`block.timestamp >= deadline`)
2. Голосование не должно быть финализировано ранее
3. Кворум должен быть достигнут: `votesFor + votesAgainst >= minimumQuorum`
4. Процент одобрения должен быть достигнут: `(votesFor * 100) / (votesFor + votesAgainst) >= minimumApprovalPercentage`

**Применение изменений при успехе:**

Если голосование успешно, в зависимости от типа:

*Для `NumericalValueVoting` и `CategoricalValueVoting`:*
```solidity
Matricies.addValue(
    matricies,
    organ,
    x,
    y,
    value,
    valueAuthor,
    isCategorical
);
```

Внутри `addValue`:
```solidity
// Упаковка: адрес автора (160 бит) + значение (64 бита) = 224 бита
uint224 packed = uint224(bytes28(abi.encodePacked(valueAuthor, value)));

// Добавление в временной ряд с меткой текущего блока
cell.numericalSample.push(uint32(block.timestamp), packed);
```

*Для `MembershipVoting`:*
```solidity
partyMembersRegistry.membersByOrgan[organ].add(member);
```

*Для `CategoryVoting`:*
```solidity
cell.allowedCategories.add(category);
cell.categoryNames[category] = categoryName;
```

*Для `ThemeVoting` и `StatementVoting`:*
```solidity
matricies.themes[isCategorical][x] = theme;
matricies.statements[isCategorical][y] = statement;
```

**События:**
При успешном добавлении значения эмитируется:
```solidity
event ValueAdded(
    uint256 indexed x,
    uint256 indexed y,
    uint64 value,
    address indexed author
);
```

**Финализация:**
```solidity
voting.finalized = true;
```

После финализации голосование нельзя исполнить повторно.

---

#### 5. Агрегация и обновление выборки

После успешного голосования:
1. Значение добавляется в `Checkpoints.Trace224` соответствующей ячейки
2. Метка времени фиксируется автоматически
3. Автор значения сохраняется в упакованном виде
4. Длина выборки увеличивается на 1

**Доступ к выборке:**
```solidity
// Получение информации о ячейке
(PartyOrgan organ, uint64[] memory categories, uint256 sampleLength) = 
    zarya.getCategoricalCellInfo(x, y);

// sampleLength — количество значений в выборке S_Y[x][y]
```

**Ретроспективный анализ:**
Благодаря `Checkpoints`, можно получить состояние выборки на любой момент времени:
```solidity
// Off-chain через чтение storage или события
uint32 timestamp;
uint224 packed;
(timestamp, packed) = cell.categoricalSample.at(index);

// Распаковка
address author = address(uint160(packed >> 64));
uint64 value = uint64(packed);
```

Это обеспечивает возможность:
- временного анализа эволюции мнения $\mathcal{M}(t)$
- атрибуции влияния конкретных участников
- построения траекторий изменения позиций партии

### Человекочитаемая репрезентация результатов агрегации

Отлично, теперь мы имеем описанные: мнение партии (формальную и эмпирическую теоретические платформы), структуру данных хранения мнения партии, структуру DAO (операционную платформу). Как теперь слепить всё воедино так, чтобы результаты работы "Зари" были человекочитаемы?

Модель мнения партии предполагает автоматическую генерацию текстовых документов на основе агрегированных значений матрицы:

$$
\hat{\mathcal{M}} = f(S_X, S_Y)
$$

В отличие от классической системы «документы + поправки», человекочитаемые документы в этой модели:

- не хранятся в виде статичных текстов;
- не изменяются вручную;
- а формируются динамически на основе текущего мнения DAO, зафиксированного в выборочных данных.

### Природа результата

Результатом работы сводной функции $f$ являются **различные описательные статистики**, полученные на основе выборочных распределений, накопленных в каждой ячейке:

- для числовых ячеек ($S_X$): средние значения, медианы, доверительные интервалы, квантильные оценки и т. д.;
- для категориальных ячеек ($S_Y$): частотные распределения, моды, вероятностные оценки по категориям и пр.

Формат представления этих значений зависит от контекста документа, но логика извлечения едина: агрегированное мнение — это производная от эмпирических выборок из матриц внутри мнения партии $M$.

### Связь с шаблонами

Каждая секция документа (например, в Markdown) может быть связана с координатами конкретной ячейки $\hat{X_{i,j}}$ или $\hat{Y_{k,l}}$ через шаблонизатор (например, Jinja2). При генерации:

- значения подставляются в текстовые фрагменты;
- логика вывода (например, «Партия считает, что…») управляется на основе правил форматирования.

### Условие однозначности

Чтобы документы можно было интерпретировать строго и однозначно, необходимо соблюдение следующих условий:

1. Каждая ячейка матрицы должна иметь фиксированное описание в ончейн-реестре (`MatrixSchemaRegistry`);
2. Шаблон должен ссылаться **только** на координаты ячейки;
3. Логика генерации документа должна быть детерминирована и открыта.

### Примеры агрегирования

#### Пример 1: Подстановка числовой оценки из $\hat{X}$

**Семантика ячейки**  
Партия считает, что средний уровень выбросов CO₂ в Челябинской области — важнейший экологический показатель.

- Координаты: `X[12][3]`
- Строка: `Экология — СОВ`
- Столбец: `Средний уровень выбросов CO₂`

**Агрегированные значения**  
- Среднее: 6.12  
- Доверительный интервал: [5.9, 6.3]

**Шаблон (Jinja2):**
```jinja2
{% set value = X["12"]["3"].mean %}
{% set ci = X["12"]["3"].confidence_interval %}
Партия считает, что средний уровень выбросов CO₂ в Челябинской области составляет {{ value }} т/чел/год 
(доверительный интервал: {{ ci[0] }} – {{ ci[1] }}).
```

**Результат:**  
Партия считает, что средний уровень выбросов CO₂ в Челябинской области составляет 6.12 т/чел/год (доверительный интервал: 5.9 – 6.3).

---

#### Пример 2: Подстановка категориального мнения из $\hat{Y}$

**Семантика ячейки**  
Совет МО №2 Московской области считает, что базовый доход — необходимая социальная мера.

- Координаты: `Y[21][0]`
- Строка: `Социальная политика — 50.2.СОВ`
- Столбец: `Отношение к базовому доходу`

**Агрегированные значения**  
- "нужна" — 68%, "сомнительно" — 22%, "опасна" — 10%

**Шаблон (Jinja2):**
```jinja2
{% set result = Y["21"]["0"].mode %}
{% set support = Y["21"]["0"].distribution[result] %}
Совет МО №2 Московской области считает, что базовый доход — "{{ result }}" 
(доля поддержки — {{ support * 100 }}%).
```

**Результат:**  
Совет МО №2 Московской области считает, что базовый доход — "нужна" (доля поддержки — 68%).

---

#### Пример 3: Предложение о приёме нового члена партии

**Семантика ячейки**  
У Иванова Ивана Ивановича сложная политическая биография. Человек сначала имел право-радикальные взгляды, затем с возрастом его взгляды умерились. Сначала он поддерживал военный конфликт в Украине, а потом его позиция поменялась. Смены позиций были зафиксированы членами нашей партии по заявлениям в СМИ или в его соц. сетях и по его проектам. Местное собрание МО №3 Нижегородской области оценивает кандидатуру Иванова Ивана Ивановича на вступление в партию.

- Координаты: `Y[33][7]`
- Строка: `Кадровые вопросы — 52.3.ОБС`
- Столбец: `Решение по приёму члена`

**Агрегированные значения**  
- "принимается" — 82%, "воздержаться" — 10%, "отказать" — 8%

**Шаблон (Jinja2):**
```jinja2
{% set decision = Y["33"]["7"].mode %}
{% set support = Y["33"]["7"].distribution[decision] %}
МО №3 Нижегородской области приняло решение: кандидатура Иванова Ивана Ивановича — "{{ decision }}" 
(за — {{ support * 100 }}% голосов).
```

**Результат:**  
МО №3 Нижегородской области приняло решение: кандидатура Иванова Ивана Ивановича — "принимается" (за — 82% голосов).

---

#### Пример 4: Приближённый к реальности и более подробный пример по изменению категориального мнения партии.

**Тема: Оценка экологических последствий рекультивации свалок в Челябинске**

**1. Контекст**

- Ячейка: `Y[15][3]`
- Строка: `Экология — 74.2.СОВ`
- Столбец: `Оценка последствий рекультивации свалок (экспертная)`
- Тип: категориальная
- Новое значение: `"приемлемо"`
- Орган голосования: собрание МО №2 Челябинской области
- Результат: голосование прошло успешно

**2. Обновление выборки**

После голосования значение добавлено в выборку:

```js
S_Y[15][3] = ["приемлемо", ..., "приемлемо"]
```

**3. Агрегация (сводная функция `f`)**

Вычисленные результаты:

```json
{
  "приемлемо": "72%",
  "сомнительно": "18%",
  "неприемлемо": "10%"
}
```

Мода: `"приемлемо"`

**4. Шаблон (Jinja2)**

```jinja2
{% set mode = Y["15"]["3"].mode %}
{% set p = Y["15"]["3"].distribution[mode] %}
Совет МО №2 Челябинской области считает, что экологические последствия рекультивации свалок являются "{{ mode }}" (поддержка — {{ p * 100 }}% DAO-участников).
```

**5. Итоговый текст**

> Совет МО №2 Челябинской области считает, что экологические последствия рекультивации свалок являются "приемлемыми" (поддержка — 72% DAO-участников).

---

#### Пример 5: Приближённый к реальности и более подробный пример по изменению непрерывного мнения партии.

**Тема: Увеличение команды OSINT и факт-чекинга в пресс-службе партии**

### 1. Контекст

- Ячейка: `X[18][2]`
- Строка: `Информационная политика — СЗД`
- Столбец: `Оценка целевой численности команды OSINT`
- Тип: непрерывная
- Код органа: `СЗД` (федеральный уровень → Съезд партии)

### 2. Процесс голосования

1. **Инициация**  
   Один из делегатов предлагает добавить новое значение в выборку: `12` (человек в команде).

2. **Определение органа**  
   DAO по коду `СЗД` определяет, что голосование должно проходить на **Съезде партии**.

3. **Проведение голосования**
   - Участвуют делегаты от более чем 50% субъектов РФ.
   - Требуется простое большинство голосов для утверждения.
   - Голосование проводится открыто.

4. **Результат**
   Голосование завершено успешно. Значение `12` утверждено.

### 3. Обновление выборки

Новое значение добавляется к выборке:

```js
S_X[18][2] = [10, 11, 12, 13, 12]
```

### 4. Агрегация (сводная функция `f`)

На основе выборки вычисляется:

- Среднее: `11.6` → округлено вниз: `11`
- Доверительный интервал (95%): `[11.0, 12.2]` → округлено вниз: `[11, 12]`

### 5. Шаблон (Jinja2)

```jinja2
{% set mean = X["18"]["2"].mean|int %}
{% set ci = X["18"]["2"].confidence_interval %}
{% set ci_lower = ci[0]|int %}
{% set ci_upper = ci[1]|int %}
Партия считает, что оптимальная численность OSINT-команды пресс-службы составляет {{ mean }} человек 
(доверительный интервал: {{ ci_lower }} – {{ ci_upper }}).
```

### 6. Итоговая человекочитаемая интерпретация

> Партия считает, что оптимальная численность OSINT-команды пресс-службы составляет 11 человек (доверительный интервал: 11 – 12).

---

## Прогнозирование политических решений и проверка нулевой гипотезы

В модели мнения партии, основанной на выборочных данных и агрегирующей функции `f`, возможна реализация механизма **предварительного прогнозирования изменения позиции** партии до голосования.

### Цель

Определить, **выполнится ли нулевая гипотеза (`H₀`)**, если в результате голосования в выборку ячейки будет добавлено новое значение.

### Формальная постановка

Пусть:

- `𝓢` — текущая выборка значений ячейки,
- `f(𝓢)` — агрегированное значение (среднее, мода, распределение и т.д.),
- `x_new` — новое значение, предложенное участником,
- `𝓢' = 𝓢 ∪ {x_new}`,
- `ĥ = f(𝓢')` — новое мнение партии.

#### Нулевая гипотеза `H₀`:
> Агрегированное мнение после обновления останется в пределах допустимого / ожидаемого значения.

### Пример: непрерывная величина

**Партия считает допустимым уровень налоговой нагрузки ≤ 15%.**

- Текущая выборка: `[13.5, 14.0, 14.2, 15.1]`
- `f(𝓢)` = `14.2` (среднее)

#### Альтернатива 1:
- Новое значение: `x_new = 15.5`
- `f(𝓢') = mean([13.5, 14.0, 14.2, 15.1, 15.5]) = 14.46`  
→ `H₀` **выполняется**

#### Альтернатива 2:
- Новое значение: `x_new = 18`
- `f(𝓢') = mean([13.5, 14.0, 14.2, 15.1, 18.0]) = 14.96`  
→ `H₀` **выполняется, но на грани**

#### Альтернатива 3:
- Новое значение: `x_new = 20`
- `f(𝓢') = mean([13.5, 14.0, 14.2, 15.1, 20.0]) = 15.36`  
→ `H₀` **нарушена**

---

### Пример: категориальная величина

**Партия считает инициативу "приемлемой", если ≥ 60% оценок — соответствующие.**

- Текущая выборка:
  - "приемлемо": 6
  - "неприемлемо": 4

- `f(𝓢)` → "приемлемо", 60%

**Новое значение:** `"неприемлемо"`  
→ новое распределение:
- "приемлемо": 6
- "неприемлемо": 5  
→ `54.5%` → `H₀` **нарушена**

---

### DAO-реализация

DAO может:

- Предварительно рассчитывать `f(𝓢')` до голосования.
- Выводить предупреждение: «добавление этого значения изменит агрегированное мнение партии».
- Использовать нарушение `H₀` как критерий:
  - для вызова расширенного кворума;
  - для запроса повторной валидации;
  - для федерализации вопроса (эскалация на уровень Съезда).

### Возможности

- **Сценарное планирование:** анализ чувствительности партийного мнения к новым голосам.
- **Обратный расчёт:** какие значения нужно собрать, чтобы мнение партии изменилось.
- **Ончейн-прогнозы:** DAO может встраивать `H₀` как параметр предложений.

### Что нам это даёт?

Проверка гипотезы при добавлении значения превращает систему из реактивной в **проактивную**: участники и органы смогут предвидеть последствия голосования **до** его проведения.

## Другие теоретические возможности анализа поведения матрицы мнения партии $\mathcal{M}(t)$

### 1. Эволюция мнения и траектории изменений

Отслеживание того, как меняются конкретные ячейки матрицы $\mathcal{M}$ во времени:
- Формирование временных рядов: $\hat{X}_t$, $\hat{Y}_t$
- Расчёт скорости изменений, выявление точек резких сдвигов
- Диаграммы направленности изменений во мнении партии

### 2. Меры стабильности и турбулентности

- Оценка волатильности значений в матрице
- Частота смены мод в категориальных ячейках
- Расчёт энтропии или дисперсии для определения неспокойных участков мнения

### 3. Консервативность vs Адаптивность DAO

- Время между предложением и изменением ячейки
- Средняя глубина изменений (по величине/категории)
- Порог чувствительности партии к новым значениям

### 4. Координационные кластеры и межрегиональные паттерны

- Кластеризация регионов по характеру голосований
- Нахождение регионов-«лидеров» и «вторящих»
- Географическое распределение согласия и расхождения по темам

### 5. Анализ консенсуса и поляризации

- Определение уровня согласованности по ячейке:
  - высокая мода — высокий консенсус
  - bimodal или равномерное распределение — поляризация
- Применение показателей энтропии и доверительных интервалов

### 6. Стресс-тесты и сценарный анализ

- Моделирование будущего состояния $\mathcal{M}$ при изменениях в голосованиях
- Проверка устойчивости мнения к системным шокам (например, синхронное изменение в нескольких регионах)

### 7. Ретроспективный анализ

- Реконструкция истории изменения ключевых позиций партии
- Отслеживание авторства инициатив и их влияния
- Анализ циклов мнения (например, возвращение к ранее отклонённым оценкам)

### 8. Метрика идеологической целостности

- Сравнение текущего состояния $\mathcal{M}$ с программой партии
- Оценка отклонения от базовой линии
- Выделение противоречивых участков или нелогичных комбинаций позиций

### 9. Фракционный анализ и предсказание внутрипартийных блоков

- Сравнение голосований и выборок между отделениями и участниками
- Кластеризация по паттернам поведения:
  - кто предлагает схожие значения?
  - кто системно расходится?
- Выявление:
  - неформальных фракций;
  - географически-идеологических крыльев;
  - потенциальных очагов напряжённости

### Используемые инструменты разработки

Для разработки демо предлагается использовать:

1) Для смарт-контрактной части (блокчейн части): язык программирования Solidity и систему сборки смарт-контрактов Hardhat.
2) Для фронтенд части (интерфейсной части): фреймворк React и набор компонентов для взаимодействия с блокчейном Wagmi.
3) Для бэкенд части (cерверной):
   1) Язык программирования: Python
   2) Для RESTful API cервера: фреймворк Django или Flask
   3) Для просчёта выборочных статистик мнения партии: фреймворк Pandas и scikit-learn.
   4) Для генерации человекочитаемой рекомендации ДАВО "Заря": любой шаблонизатор на языке Python.
4) Для базы данных шаблонов рекомендаций: публичная GitHub организация под рабочим названием ДАВО "Заря" и репозиторий с названием "Шаблоны". 